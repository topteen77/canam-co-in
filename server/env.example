# Server reads the project-root .env (this file is a symlink: server/.env -> ../.env).
# Edit globals in: ../.env
# Template: ../.env.example
#
# If this symlink is missing, run: ln -sfn ../.env .env
