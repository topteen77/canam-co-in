# Optional local overrides for Vite (loaded after .env).
# Prefer setting website globals in the project-root .env instead.
#
# GEMINI_API_KEY=your_gemini_api_key_here
# VITE_API_URL=http://localhost:5001/api
